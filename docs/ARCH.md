

- Based on how far neurons are from each other, they should tick faster or slower.
- On static classification tasks (e.g., MNIST), when do we stop ticking? Do we have a "thinking time" parameter for inference?
- The brain should have a velocity parameter (how much each tick moves the weights)



- Add lib use examples
- Public and local benchmarks. Start with simple linear regression, then polynomial, then mnist, etc.